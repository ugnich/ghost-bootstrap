<div class="hero-section {{#if @site.cover_image}} has-cover{{else}} no-cover-image{{/if}}"{{#if @site.cover_image}} style="background-image: url({{@site.cover_image}})"{{/if}}>
    <div class="container">
        <div class="row">
            <div class="col-md-10 mx-auto text-center position-relative">
                <h1 class="display-4 text-white">{{@site.title}}</h1>
                <p class="lead text-white">{{@site.description}}</p>
            </div>
        </div>
    </div>
</div>